NEXT_SESSION.txt — Liminal Sin Gemini
Generated: March 14, 2026
Purpose: Full context handoff + implementation plan for next session.
This file exists because the previous chat window ran out of context.

================================================================================
SECTION 1 — SESSION SUMMARY (what was done today)
================================================================================

All work is committed and deployed. Cloud Run revision liminal-sin-server-00045-x72
is live and serving 100% of traffic at:
  https://liminal-sin-server-1071754889104.us-west1.run.app

COMPLETED THIS SESSION:
- gmTools.ts  — triggerAcecardReveal added (Option A broad semantic match keyword)
- gmTools.ts  — triggerSceneChange sceneKey list updated with 5 new canonical keys
- gmTools.ts  — triggerDreadTimerStart corrected to "acecard keyword window / step 31 only"
- SHOT_SCRIPT.md — Phase 6B rewritten (park_liminal chained_auto beat)
- SHOT_SCRIPT.md — Phase 6C added (park_shaft_view decision beat)
- SHOT_SCRIPT.md — Phase 7 rewritten (elevator descent + maintenance corridor, steps 26/27/28/29)
- SHOT_SCRIPT.md — Phase 7B added (acecard keyword gate, both outcome paths)
- SHOT_SCRIPT.md — Media Filename Registry 8 stale rows corrected
- TypeScript: zero errors. ESLint: zero errors.
- IMPLEMENTATION_PLAN.txt deleted after all sections A-F confirmed done.
- Stale file audit completed. 25 files/dirs deleted:
    - Temp/log: tmp_part.txt, tmp_run_logs.txt, server_output.log, tmp_server_err.log
    - Next.js scaffold: .next/ dir + 5 SVG template assets in public/
    - Debug scripts: debug-veo.ts, vanilla-veo-test.ts
    - Test artifacts: scripts/output/* (5 paths)
    - Stale backup: docs/SHOT_SCRIPT.md.bak
    - Superseded planning docs: PROMPT_QUEUE_SEQUENCE.md, PROMPT_LIBRARY_IMAGE.md,
      PROMPT_LIBRARY_VIDEO.md, Player_voice.md
    - Demo prompt: prompts/jason.demo.md (confirmed deleted today)

CURRENT_STATE.md updated with full March 14 status delta.

================================================================================
SECTION 2 — CONTEXT BLOAT ANALYSIS (why the window fills up fast)
================================================================================

Total source file footprint (excl. node_modules, dist, .git, .next, assets): ~843 KB

TOP OFFENDERS (sorted by size):
  63 KB   docs/SHOT_SCRIPT.md        (714 lines)
  53 KB   server/server.ts           (1603 lines — OVER 800-line file cap)
  37 KB   public/ls/lsr.html         (duplicate of public/lsr.html — identical twin)
  37 KB   public/lsr.html
  28 KB   docs/Characters.md         (358 lines — stale model names inside)
  26 KB   docs/WORLD_BIBLE.md        (334 lines)
  19 KB   docs/Gamemaster.md         (279 lines)
  18 KB   server/services/gameMaster.ts (well within cap)
  16 KB   scripts/pipeline-variant-benchmark.ts
  16 KB   server/services/veo.ts
  16 KB   CURRENT_STATE.md           (191 lines — growing with each delta)
  15 KB   server/services/gemini.ts
  15 KB   server/services/imagen.ts
  12 KB   docs/Contest.md            (163 lines — contest is over after March 16; obsolete)

ROOT CAUSES:
1. server/server.ts at 1603 lines is the single biggest source. It violates the
   300-400 line module cap with no exception on file. Should be split.
2. docs/SHOT_SCRIPT.md at 714 lines is allowed up to 800 (AGENTS.md exception).
   Still large but within policy.
3. CURRENT_STATE.md is growing with each status delta — growing log pattern.
4. docs/Contest.md (163 lines) will become pure dead weight after March 16 5PM.
5. public/lsr.html and public/ls/lsr.html appear to be identical twins (both 37KB).
   One is likely a leftover duplicate.

ACTION ITEMS (see Section 3 below for full plan):
- server.ts MUST be split into focused modules (Plan item #4)
- Contest.md can be archived post-deadline
- Verify lsr.html duplication
- CURRENT_STATE.md: switch from append-log to compact-current-state format

================================================================================
SECTION 3 — IMPLEMENTATION PLAN (all pending fixes, ordered by severity)
================================================================================

Each item below is a standalone micro-step. Execute in order.
After each step: npx tsc --noEmit + npx eslint <file> — then commit.

--------------------------------------------------------------------------------
PLAN ITEM 1 — HIGH SEVERITY
File: server/services/gameMaster.ts
Function: resolveTriggerType()
Issue: holdIds Set is missing 3 new scene keys added in Section B (last session).
       The GM can spontaneously call triggerSceneChange with these keys via its
       own function declarations. When it does, the frontend receives
       triggerType: "chained_auto" and auto-advances instead of waiting for
       player input.

Fix — add to the holdIds Set:
  CURRENT (line ~82-88):
    const holdIds = new Set([
      "card_joker_01",
      "card_pickup_01",
      "card_pickup_02",
      "hallway_pov_01",
      "maintenance_reveal_01",
    ]);

  REPLACE WITH:
    const holdIds = new Set([
      "card_joker_01",
      "card_pickup_01",
      "card_pickup_02",
      "hallway_pov_01",
      "hallway_pov_02",
      "elevator_inside_01",
      "elevator_inside_02",
      "maintenance_reveal_01",
    ]);
--------------------------------------------------------------------------------

--------------------------------------------------------------------------------
PLAN ITEM 2 — HIGH SEVERITY
File: server/services/gameMaster.ts
Function: resolveTimeoutSeconds()
Issue: Returns stale timeout values that diverge from the SHOT_SCRIPT canonical
       Media Filename Registry (corrected last session). The step machine in
       server.ts has the correct values; this function was not updated.

  CURRENT (line ~96-108):
    function resolveTimeoutSeconds(mediaId: string): number {
      if (
        mediaId === "card_joker_01" ||
        mediaId === "card_pickup_01" ||
        mediaId === "card_pickup_02"
      ) {
        return 25;
      }
      if (mediaId.includes("maintenance") || mediaId.includes("hallway")) {
        return 15;
      }
      return 30;
    }

  REPLACE WITH:
    function resolveTimeoutSeconds(mediaId: string): number {
      if (mediaId === "card_joker_01" || mediaId === "card_pickup_01") {
        return 30;
      }
      if (mediaId === "card_pickup_02") {
        return 15;
      }
      if (mediaId === "hallway_pov_01") {
        return 15;
      }
      return 30;
    }

  RATIONALE:
    card_joker_01:   25 -> 30  (SHOT_SCRIPT registry row 133)
    card_pickup_02:  25 -> 15  (SHOT_SCRIPT registry row 136; 15s card window)
    hallway_pov_02:  was caught by includes("hallway") = 15; now returns 30 from default
    maintenance IDs: were returning 15; all maintenance IDs should return 30 (default)
--------------------------------------------------------------------------------

--------------------------------------------------------------------------------
PLAN ITEM 3 — HIGH SEVERITY
File: server/services/gemini.ts
Function: getGameMasterSystemPrompt() — the demoSequence constant
Issue: The embedded GM playbook predates the acecard mechanic (Section A) and the
       new steps 24/26/28/31 (Section B). Three specific problems:

  3A — triggerAcecardReveal is declared in gameMasterTools and available to the GM
       but NEVER mentioned in the system prompt. A Gemini agent rarely spontaneously
       uses a tool it has no instructions for. The GM will never fire acecard reveal.

  3B — Beat 7 instruction says:
         "Call triggerDreadTimerStart with durationMs 30000"
       under the maintenance/card2 context. But the tool description (fixed last
       session) now says "step 31 only — do not call during earlier maintenance steps."
       These are directly contradictory. The system prompt overrides the tool
       description — the GM will fire dread timer at Beat 7, not step 31.

  3C — CROSS-BEAT RULES scene key list is missing 5 keys:
       park_liminal, elevator_inside, elevator_inside_2, hallway_pov_02, acecard_reveal

  FIX STRATEGY:
    In the demoSequence string inside getGameMasterSystemPrompt():

    A — Append to BEAT 7 block (after card2_pickup_pov line):
        - Call triggerDreadTimerStart ONLY at step 31 (hallway_pov_02 still shown).
          Do NOT call it during maintenance panel or earlier card2 steps.
        - At step 31 (hallway_pov_02), watch for any grab/take/pick-up instruction.
          When detected, call triggerAcecardReveal immediately. This is the only
          correct trigger for triggerAcecardReveal.

    B — In BEAT 7, remove or qualify the dread timer instruction so it reads:
        "Do NOT call triggerDreadTimerStart here. See Beat 7B."

    C — Add a BEAT 7B block (insert between BEAT 7 and BEAT 8):
        BEAT 7B - ACECARD KEYWORD GATE (step 31)
        - Trigger cue: hallway_pov_02 is showing and acecard keyword window is open.
        - Watch for any instruction meaning grab/take/pick-up/retrieve.
        - Call triggerAcecardReveal when detected.
        - Call triggerDreadTimerStart with durationMs 30000 when hallway_pov_02 first shows.
        - If timer expires before detection: backend fires game_over. No further calls.
        - If triggerAcecardReveal fires: do nothing further. Backend handles acecard_reveal clip.

    D — In CROSS-BEAT RULES, append to scene key list:
        ", park_liminal, elevator_inside, elevator_inside_2, hallway_pov_02, acecard_reveal"

  NOTE: The system prompt is a long string. Read the full function before editing.
        File: server/services/gemini.ts — function getGameMasterSystemPrompt()
        The demoSequence constant is defined inside that function.
        Do not touch anything outside of demoSequence.
--------------------------------------------------------------------------------

--------------------------------------------------------------------------------
PLAN ITEM 4 — MEDIUM SEVERITY (code health / context bloat)
File: server/server.ts (1603 lines — violates 800-line exception cap)
Issue: server.ts is 1603 lines. The AGENTS.md 800-line exception applies to docs
       only (CURRENT_STATE.md, README.md, AGENTS.md, SHOT_SCRIPT.md). Source code
       caps are 300/400 lines per module category. This file is ~4x over that limit.

  SPLIT PLAN:
    Extract to server/services/stepMachine.ts (~250 lines):
      - STEP_MEDIA_TRIGGER record + getStepTimeoutSeconds()
      - autoplayAdvance() closure (the full advance logic including all handlers
        for steps 7/9/11/13/17/19/21/23/24/25/26/27/28/29/31)
      - clearCardAutoPickTimers()
      - clearWildcardTimers() (the parts that don't need ws/jasonManager captures)

    Extract to server/services/acecardGate.ts (~120 lines):
      - startAcecardKeywordTimer()
      - handleAcecardReveal()
      - startCardPickup02Timer()
      - Related state variables: acecardKeywordTimer, acecardKeywordReceived,
        cardPickup02Timer

    What stays in server.ts:
      - WS server bootstrap, Express setup, healthcheck route
      - Per-connection session lifecycle (openLiveSession, onFunctionCall, onAgentAudio)
      - WS message router (the big switch on msg.type)
      - Top-level timer/state vars for connection scope
      - prewarm calls and wildcard pipeline triggers

  PREREQUISITE: Do Plan Items 1-3 first. Split server.ts last.
  RISK: Medium. This is a non-trivial refactor. Requires full typecheck after.
  AGENTS.md RULE: Always ask user before making changes that touch more than one
  file or module. Confirm before starting this item.
--------------------------------------------------------------------------------

--------------------------------------------------------------------------------
PLAN ITEM 5 — MEDIUM SEVERITY
File: server/services/imagen.ts
Issue: vision_flash entry in ZONE_PROMPTS is a label string, not a prompt.
       If any code path resolves this key, Imagen 4 receives the label as its
       generation prompt, producing garbage or a 400 error.
       The keyword fallback also routes "flash" keys here before hitting the
       wildcard_vision_feed fallback.

  FIX: Replace the vision_flash entry with the wildcard_vision_feed prompt,
       or remove the entry entirely and let the fallback handle it.

  RECOMMENDED: Remove the vision_flash key from ZONE_PROMPTS entirely.
  The resolvePrompt() fallback chain will correctly route any "flash"-containing
  key through to the wildcard_vision_feed path before reaching the default.

  VERIFY: After removing vision_flash from ZONE_PROMPTS, trace the fallback:
    key.includes("vision") || key.includes("flash") → ZONE_PROMPTS["wildcard_vision_feed"]
  This is in the keyword fallback block below the direct-match loop. ✓
--------------------------------------------------------------------------------

--------------------------------------------------------------------------------
PLAN ITEM 6 — MEDIUM SEVERITY
File: server/services/veo.ts
Issue: Same pattern as Plan Item 5. vision_flash in ANIMATION_HINTS is a label
       string. Same keyword fallback risk.

  FIX: Remove the vision_flash key from ANIMATION_HINTS entirely.
  The resolveAnimationHint() fallback:
    key.includes("vision") || key.includes("flash") → ANIMATION_HINTS["vision_flash"]
  ...routes BACK to the now-deleted key. This creates a runtime KeyError.

  CRITICAL: When removing vision_flash from ANIMATION_HINTS, also update the
  fallback in resolveAnimationHint() to point to wildcard_vision_feed instead:
    if (key.includes("vision") || key.includes("flash"))
      return ANIMATION_HINTS["wildcard_vision_feed"];
--------------------------------------------------------------------------------

--------------------------------------------------------------------------------
PLAN ITEM 7 — LOW SEVERITY
File: server/services/imagen.ts
Issue: JSDoc comment on resolvePrompt() says:
         "Format reminder: {character}_{emotion}_{context}_{action}"
       This is the exact format that was removed from gmTools.ts in the last
       session because it was teaching the GM to hallucinate freeform scene keys.
       It's a comment, not runtime, but it is now misleading/contradictory.

  FIX: Delete or replace this single comment line.
  REPLACE WITH: "Canonical Act 1 scene keys only. See gmTools.ts triggerSceneChange."
--------------------------------------------------------------------------------

--------------------------------------------------------------------------------
PLAN ITEM 8 — LOW SEVERITY
File: server/services/npc/jason.ts
Issue: JSDoc comment says:
         "Canonical prompt source: prompts/jason.demo.md"
       prompts/jason.demo.md was deleted today. This is now a dangling reference.

  FIX: Remove or update this comment line.
  REPLACE WITH nothing (delete the line) or:
         "Prompt lives here. prompts/ directory has been removed."
--------------------------------------------------------------------------------

--------------------------------------------------------------------------------
PLAN ITEM 9 — LOW SEVERITY (docs)
File: docs/Characters.md
Issues (non-breaking, doc accuracy):
  A — Agent architecture table (lines ~21-24) lists:
        gemini-2.0-flash-live-preview-04-09
      for all 4 agents. Live model is: gemini-live-2.5-flash-native-audio
      AGENTS.md §9 explicitly flags this as stale.

  B — Line ~39: "Imagen 3 scene triggers"
      Line ~41: "Pre-generated FMV clips → Imagen 3 live generation"
      Project is on imagen-4.0-generate-001.

  FIX: Update all 4 model name cells in the agent table to gemini-live-2.5-flash-native-audio
       Update "Imagen 3" to "Imagen 4" (2 occurrences).
  NOTE: Per AGENTS.md §9, Characters.md model names are explicitly called out as
        stale. This brings the doc into sync. Low risk, no runtime impact.
--------------------------------------------------------------------------------

--------------------------------------------------------------------------------
PLAN ITEM 10 — DELETE entire public/ directory
Scope: public/ directory — 9 files/dirs, ~120KB total
Issue: This entire directory is myceliainteractive FRONTEND deployment artifacts
       that were accidentally copied into the backend repo.

  FILES TO DELETE (all of them — none are served or deployed):
    public/lsr.html             (37KB — game UI shell; IDENTICAL to public/ls/lsr.html)
    public/ls/lsr.html          (37KB — duplicate)
    public/privacy.html         (4.5KB — IDENTICAL to public/ls/privacy.html)
    public/ls/privacy.html      (4.5KB — duplicate)
    public/privacy_policy.md    (4.3KB — IDENTICAL to public/ls/privacy_policy.md AND PRIVACY_POLICY.md)
    public/ls/privacy_policy.md (4.3KB — triple duplicate)
    public/ls/index.html        (0.4KB — redirect shim to lsr.html)
    public/_redirects            (2 lines — Cloudflare Pages routing rules; backend has no Cloudflare)
    public/ls/                  (directory)
    public/                     (directory)

  CONFIRMED SAFE TO DELETE BECAUSE:
    - Express server has zero static middleware (no express.static() call)
    - server.ts has no sendFile, no HTML routes, no references to /public
    - Dockerfile COPYs only server/ and tsconfig.json — never public/
    - _redirects is a Cloudflare Pages artifact; Cloud Run ignores it entirely
    - All duplicates verified byte-identical via Compare-Object
    - Canonical copies live in myceliainteractive repo (the correct frontend repo)
    - PRIVACY_POLICY.md at project root is the retained canonical copy

  COMMAND:
    Remove-Item "public" -Recurse -Force

  ORIGINAL AUDIT MISS REASON:
    The first stale-file audit identified these as "backend server has no
    express.static()" but categorized them as the lsr.html investigation rather
    than flagging the entire public/ directory for immediate deletion.
    Root cause: audit was structured around identifying Next.js scaffold remnants
    (Category 2) and did not include a "frontend assets in a backend repo" category.
    Rule to add to future audits:
      CHECK: Does any file in public/ get served by the Express routes?
      If no express.static() or sendFile() exists, the entire public/ directory
      is a stray frontend artifact and should be deleted.
--------------------------------------------------------------------------------

--------------------------------------------------------------------------------
PLAN ITEM 11 — CURRENT_STATE.md: Replace append-log with current-state-only format
File: CURRENT_STATE.md
Issue: The file is a growing append-log. Every session adds a new "Status Delta"
       block below the previous ones. This means AI reads all historical deltas
       every session even though only the CURRENT state matters.
       At 191 lines / 16KB today, it will exceed its 800-line exception cap before
       the contest deadline.

  RULE TO ENFORCE (add to AGENTS.md §7 or CURRENT_STATE.md header):
    "When updating CURRENT_STATE.md, REMOVE the previous status delta content
     and REPLACE it with a single current-state snapshot. Do NOT append.
     Historical logs belong in git history, not in the working file."

  ACTION THIS SESSION: Rewrite CURRENT_STATE.md to reflect only current state.
    - Keep: deadlines, infrastructure table, WS event contract, active directives
    - Remove: all completed "Status Delta" log entries from March 11/12/13/14
    - Replace with: single CURRENT STATE section listing what is live right now
    - Target size: under 80 lines
--------------------------------------------------------------------------------

--------------------------------------------------------------------------------
PLAN ITEM 12 — Contest.md: Archive after deadline
File: docs/Contest.md (12.2KB, 163 lines)
Issue: Contest.md contains submission strategy and scoring intelligence for the
       Google Gemini Live Agent Challenge. Its hard deadline is March 16, 2026
       5:00 PM PDT. After that moment, the entire file is dead weight.

  NOTE ON RULES CHANGE:
    AGENTS.md §9 says Contest.md should be read "Only when evaluating submission
    requirements or deadlines." After March 16, this condition never fires again.
    A rule should be added to AGENTS.md to strip time-bounded reference docs
    after their deadline passes rather than letting them accumulate.

    SUGGESTED AGENTS.md ADDITION (§9 or new §10):
      "Time-bounded reference documents (Contest.md and similar) must be removed
       or archived after their deadline passes. Do not leave expired constraint
       docs in the active repo — they add context overhead with zero benefit."

  ACTION: After March 16 5PM PDT — rename docs/Contest.md. to docs/ignoreme.txt
  PRE-DEADLINE: Do not touch it. It is still required for submission scoring.
--------------------------------------------------------------------------------

================================================================================
SECTION 4 — EXECUTION ORDER
================================================================================

  Start here (next session):
    1. Plan Item 10 (delete public/ directory)         — 2 min, zero risk, DO FIRST
    2. Plan Item 1  (gameMaster.ts holdIds)            — 5 min, low risk
    3. Plan Item 2  (gameMaster.ts timeouts)           — 5 min, low risk
    4. Plan Item 5  (imagen.ts vision_flash)           — 5 min, low risk
    5. Plan Item 6  (veo.ts vision_flash + fix fallback) — 5 min, medium risk
    6. Plan Item 7  (imagen.ts stale comment)          — 2 min
    7. Plan Item 8  (jason.ts dangling comment)        — 2 min
    8. Plan Item 9  (Characters.md model names)        — 5 min
    9. Plan Item 11 (CURRENT_STATE.md rewrite)         — 10 min, confirm with user
   10. Plan Item 3  (gemini.ts GM playbook)            — 15 min, most complex
   11. Plan Item 4  (server.ts split)                  — confirm with user first
   12. Plan Item 12 (Contest.md — after deadline only) — March 16 5PM PDT

  After each group of 2-3 items:
    npx tsc --noEmit
    npx eslint server/services/<changed-file>.ts
    git add . && git commit -m "fix: <item description>"

  After all items complete:
    npm run deploy

================================================================================
SECTION 5 — SYSTEM INVARIANTS REMINDER (do not violate)
================================================================================

  - GM never speaks — function calls only. Never route GM audio to client.
  - Trust is float 0.0-1.0. Three-state enum is a UI alias only.
  - scene_key format: flat descriptive keys only (registry in SHOT_SCRIPT.md).
  - Slotsky: no speech, no appearance, no single-character targeting.
  - Jason: HARDCODED refusal of suicidal commands at any trust level.
  - Audrey: silent (not hostile) at trust < 0.2.
  - USB drive: Act 2 content. Do not unlock or reference in Act 1 logic.
  - No menus, no pause, no HUD during play. Exception: WILDCARD1, GAME OVER and GOOD ENDING.
  - ADK/AutoFlow NOT implemented. Direct GenAI SDK + WebSocket only.
  - All state in Firestore. Media in GCS. No migration without approval.
  - Lyria 3 audio deferred. No audio generation until AUDIO_DESIGN.md exists.
  - Hard deadline: March 16, 2026 5:00 PM PDT.

================================================================================
NOTE: WILDCARD1 requires  HUD on the screen showing a video feed of the Player and a shadow figure in the room.

EOF
